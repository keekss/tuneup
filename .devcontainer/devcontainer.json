{
    "name": "Base",
    "image": "ghcr.io/keekss/codespaces-base:latest",
    "containerEnv": {
        "CODESPACE_ENV": "base",
        "DEVCONTAINER_NAME": "Base"
    }
    // Extensions are listed in .vscode/extensions.json
    // since they are shared for all devcontainers
}
